name: ayotani
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter_map: ^6.0.0
  latlong2: ^0.9.0
  geolocator: ^10.1.0 # Optional but good for 'My Location' button
  flutter:
    sdk: flutter
  # Existing dependencies
  flutter_svg: ^2.0.10

  video_player: ^2.9.1

  youtube_player_flutter: ^9.0.0
  
  # New Supabase & UI dependencies
  supabase_flutter: ^2.8.0
  google_fonts: ^6.2.0
  cached_network_image: ^3.4.0
  intl: ^0.20.2
  
  # State management
  provider: ^6.0.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
  assets:
    - assets/icons/google.svg
    - assets/icons/apple.svg